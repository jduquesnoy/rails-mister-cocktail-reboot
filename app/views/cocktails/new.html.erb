<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
        <h1>New_____Cocktails</h1>

        <%= simple_form_for(@cocktail) do |f| %>
          <div class="description-text">Name</div>
          <%= f.input :name, label:false %>
          <div class="description-text">photo</div>
          <%= f.input :photo, as: :file, label:false %>
          <%= f.submit "submit the new cocktail", class: "description-btn"%>
        <% end %>
      </div>
    </div>
  </div>
